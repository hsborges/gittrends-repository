{
  "name": "@gittrends/env-config",
  "version": "3.0.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "license": "MIT",
  "scripts": {
    "build": "yarn clean && yarn tsc",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "dotenv": "^8.2.0",
    "find-up": "^5.0.0"
  }
}
