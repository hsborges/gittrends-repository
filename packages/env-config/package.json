{
  "name": "@gittrends/env-config",
  "version": "3.0.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "license": "MIT",
  "scripts": {
    "build": "rm -rf dist && yarn tsc",
    "prettier": "prettier --write --config ../../.prettierrc.yml --ignore-path ../../.prettierignore  '**/*.ts'"
  },
  "dependencies": {
    "dotenv": "^10.0.0",
    "find-up": "^5.0.0"
  }
}
