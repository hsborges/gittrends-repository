{
  "name": "@gittrends/database-config",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "private": true,
  "scripts": {
    "postgres:migrate": "knex migrate:latest",
    "mongo:migrate": "migrate up --env ../../.env --migrations-dir mongo_migrations --state-file ../../.migrate"
  },
  "dependencies": {
    "dotenv": "^8.2.0",
    "knex": "^0.21.3",
    "mongodb": "^3.6.0",
    "pg": "^8.4.1"
  },
  "devDependencies": {
    "commitizen": "^4.1.2",
    "migrate": "^1.7.0"
  }
}
