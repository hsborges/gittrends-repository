/*
 *  Author: Hudson S. Borges
 */
declare global {
  type WriterFunction = (content: any, destination: string) => void;
}

// If this file has no import/export statements (i.e. is a script)
export {};
